services:
  goblin:
    build:
      context: .
      args:
        GIT_COMMIT: ${GIT_COMMIT:-unknown}
    container_name: goblin-bot
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
